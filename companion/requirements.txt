# The companion installs these itself on first run — this file is only for
# people who prefer to do it manually.
websockets>=12.0     # link to the Leviathan brain
psutil>=5.9          # vitals, process list
mss>=9.0             # screen capture

# Anything a specific capability needs (yt-dlp, pymupdf, pandas,
# faster-whisper, pyautogui, ...) is installed on demand at runtime via the
# `install_packages` tool — nothing to add here.
